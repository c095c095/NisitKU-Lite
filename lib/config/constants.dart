import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF06D001);
  static const Color secondary = Color(0xFF9BFE9A);
  static const Color accent = Color(0xFF9BEC00);
}

// class AppStyles {
//   static const BorderRadius borderRadius = BorderRadius.all(Radius.circular(30.0));
//   static const EdgeInsets padding = EdgeInsets.symmetric(horizontal: 24.0, vertical: 12.0);
// }
